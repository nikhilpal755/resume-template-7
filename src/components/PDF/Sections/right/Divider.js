import { View } from '@react-pdf/renderer'

const Divider = () => (
  <View
    style={{
      height: '1px',
      marginTop: '8px',
      marginBottom: '8px',
      width: '100%',
      backgroundColor: '#909090',
    }}
  ></View>
)

export default Divider
